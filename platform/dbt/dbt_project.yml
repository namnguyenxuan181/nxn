name: nxn
version: "1.0.0"
config-version: 2

profile: nxn

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  nxn:
    staging:
      +materialized: view
      +schema: staging
    mart:
      +materialized: table
      +schema: mart
